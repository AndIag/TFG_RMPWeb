'use strict';

describe('RestMaPla.view-dashboard module', function () {

    beforeEach(module('RestMaPla.view-dashboard'));

    describe('view-dashboard controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view2Ctrl = $controller('DashboardCtrl');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});