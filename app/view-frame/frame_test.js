'use strict';

describe('RestMaPla.view-frame module', function () {

    beforeEach(module('RestMaPla.sidebar'));

    describe('view-frame controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var sidebarController = $controller('FrameCtrl');
            expect(sidebarController).toBeDefined();
        }));

    });
});