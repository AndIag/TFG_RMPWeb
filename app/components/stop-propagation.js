'use strict';

// angular.module('RestMaPla.directives', [])
//
// .directive('stopPropagation', () => ({
//     restrict: 'A',
//     link: (scope, elem) => elem.on('click', event => event.stopPropagation())
// }));